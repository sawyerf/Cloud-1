---
- name: Deploy PHPMyAdmin
  community.docker.docker_container:
    name: '{{ pma_name }}'
    image: phpmyadmin
    ports:
      - 8081:80
    networks:
      - name: '{{ network_name }}'
    env:
      PMA_HOST: '{{ sql_name }}'
    restart_policy: always