---
- name: Install
  hosts: web
  remote_user: root
  vars: 
    db_name: wp_db
    db_user: wp_user
    db_pass: pass

    host: cloud.local
    wp_name: Wordpress
    pma_name: PHPMyAdmin
    ddb_name: DB
    network_name: nw_default

  roles:
    # - utils
    - docker
    - firewall
...